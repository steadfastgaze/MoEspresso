[project]
name = "moespresso"
version = "3.0.0"
description = "Run large Mixture-of-Experts language models well on the memory you have."
readme = "README.md"
license = "MIT OR Apache-2.0"
license-files = ["LICENSE-MIT", "LICENSE-APACHE-2.0", "THIRD-PARTY-NOTICES"]
requires-python = ">=3.11"
authors = [{ name = "Riccardo Chiumiento" }]
keywords = [
    "apple-silicon",
    "inference",
    "llm",
    "mixture-of-experts",
    "mlx",
    "quantization",
]
classifiers = [
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Operating System :: MacOS :: MacOS X",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "jang>=2.5.29",
    "mlx==0.31.2",
    "mlx-iqk==0.1.3",
    "mlx-kquant @ git+https://github.com/steadfastgaze/mlx-kquant.git@c4928ba74b6b119c3613e1ec68ef85d2238c636b",
    "mlx-lm==0.31.3",
    "numpy>=1.26",
    "psutil>=7.0.0",
]

[project.urls]
Homepage = "https://github.com/steadfastgaze/MoEspresso"
Repository = "https://github.com/steadfastgaze/MoEspresso"
Documentation = "https://github.com/steadfastgaze/MoEspresso#readme"
Issues = "https://github.com/steadfastgaze/MoEspresso/issues"

[project.optional-dependencies]
# Optional build/conversion helper dependency. This is intentionally not part of
# the runtime installation: serving does not need torch, but source/package
# tooling may need safetensors' torch path for BF16 and other non-NumPy-friendly
# dtypes.
build = [
    "torch>=2.5; sys_platform == 'darwin'",
]

[project.scripts]
# User-facing command group. The hyphenated runtime commands remain
# compatibility aliases backed by the same parsers.
moespresso = "moespresso.cli:main"
# One-shot generate from a package (prompt in, text out); no integrity
# verification, see moespresso-verify.
moespresso-generate = "moespresso.runtime.serve:main"
# Same seam, exposed as a long-running OpenAI-compatible HTTP server.
moespresso-serve = "moespresso.serve_supervisor:main"
# On-demand integrity gate (sha256)
moespresso-verify = "moespresso.runtime.serve:verify_main"
# Remote Hugging Face model/header inspection without downloading full weights.
moespresso-hf-inspect = "moespresso.inventory.hf_inspect:main"
# Alias for the short standalone inspector command.
hf-model-inspect = "moespresso.inventory.hf_inspect:main"
# Validate external DeepSeek-V4 Q1 parity evidence.
moespresso-ds4-q1-validate = "moespresso.correctness.deepseek_v4.parity:main"
# Manual-only DeepSeek-V4 quality gates. Never called by make test or CI.
moespresso-ds4-quality = "moespresso.correctness.deepseek_v4.quality:main"
# Manual-only DeepSeek-V4 teacher-forced WikiText perplexity gate: the
# natural-text arm of the acceptance bar, with a configured corpus and a
# declared limit.
moespresso-ds4-wikitext-ppl = "moespresso.correctness.deepseek_v4.wikitext_ppl:main"
# Manual-only DeepSeek-V4 Q4 KL panel over teacher and candidate logit dumps.
moespresso-ds4-q4 = "moespresso.correctness.deepseek_v4.q4:main"
# Manual DeepSeek-V4 package builder from a GGUF K-quant recipe.
moespresso-ds4-kquant-package = "moespresso.package.deepseek_v4.kquant_package:main"
# Manual DeepSeek-V4 package builder from converted IQ_K routed-expert artifacts.
moespresso-ds4-iqk-package = "moespresso.package.deepseek_v4.iqk_package:main"
# Manual released Qwen4 text builder from calibrated IQ_K expert cells.
moespresso-qwen4-iqk-package = "moespresso.package.qwen4.iqk_package:main"
moespresso-qwen4-mtp = "moespresso.runtime.qwen4.mtp_generate:main"
# Manual released Qwen4 routed-expert conversion to package-ready IQ_K rows.
moespresso-qwen4-iqk-convert = "moespresso.package.qwen4.iqk_convert:main"
# Manual relayout of a built IQ_K package's routed bundles onto the decode
# kernels' wire, without re-encoding a byte.
moespresso-ds4-iqk-relayout = "moespresso.package.deepseek_v4.iqk_relayout:main"
# Build a new physical package containing only a validated per-layer IQ_K
# expert selection, without decoding or re-encoding retained rows.
moespresso-ds4-iqk-reap = "moespresso.package.deepseek_v4.iqk_reap:main"
# Manual DSpark draft-model sidecar builder from the DSpark HF snapshot.
moespresso-ds4-dspark-sidecar = "moespresso.package.deepseek_v4.dspark_sidecar:main"
# Bundle a built DSpark sidecar into a DeepSeek-V4 package as the declared
# optional drafter component (hard links; no bytes re-encoded).
moespresso-ds4-dspark-bundle = "moespresso.package.deepseek_v4.dspark_bundle:main"
# Manual DFlash draft-model sidecar builder from the speculator HF snapshot.
moespresso-ds4-dflash-sidecar = "moespresso.package.deepseek_v4.dflash_sidecar:main"
# A/B replay of speculative decoding against plain greedy decoding. The
# CLI name predates the drafter-generic loop; `--drafter` selects the family.
moespresso-ds4-dspark-replay = "moespresso.correctness.deepseek_v4.spec_replay:main"
# Manual multi-prompt speculative-decoding measurement battery: one target
# load, every available drafter sidecar, per-arm and per-repeat statistics.
moespresso-ds4-spec-battery = "moespresso.correctness.deepseek_v4.spec_battery:main"
# Manual Qwen package builder from a GGUF K-quant recipe.
moespresso-qwen-kquant-package = "moespresso.package.qwen.kquant_package:main"
# Manual Qwen 35B exact-answer package comparison harness.
moespresso-qwen35-hard-questions = "moespresso.correctness.qwen35.hard_questions:main"
# Manual Ornith quality gate v2: reasoning, agentic coding, long-context recall.
moespresso-ornith-gate = "moespresso.correctness.ornith.gate:main"
# Manual DeepSeek-V4 speed snapshot: generation plus SSD structural counters.
moespresso-ds4-speed-stats = "moespresso.runtime.deepseek_v4.speed_stats:main"

[dependency-groups]
dev = [
    "pytest>=7.0.0",
    "ruff>=0.1.0",
    "vulture>=2.11",
]

[build-system]
requires = ["scikit-build-core==1.0.3", "nanobind==2.12.0", "mlx==0.31.2"]
build-backend = "scikit_build_core.build"

[tool.scikit-build]
minimum-version = "build-system.requires"
cmake.version = ">=3.27"
cmake.build-type = "Release"
cmake.define.CMAKE_OSX_DEPLOYMENT_TARGET = "26.2"
cmake.define.CMAKE_OSX_ARCHITECTURES = "arm64"
wheel.packages = ["src/moespresso"]
wheel.reproducible = true
wheel.exclude = ["**/private/**", "**/__pycache__/**", "**/*.pyc", "**/*.pyo"]
editable.rebuild = false
sdist.exclude = [
    ".research/**",
    "specs_archive/**",
    "native/*/build/**",
    "**/private/**",
    "**/__pycache__/**",
    "**/*.pyc",
    "**/*.pyo",
    "**/*.so",
    "**/*.dylib",
    "**/*.metallib",
    "**/*.a",
    "**/*.o",
]

[tool.uv]
cache-keys = [
    { file = "pyproject.toml" },
    { file = "README.md" },
    { file = "LICENSE-MIT" },
    { file = "LICENSE-APACHE-2.0" },
    { file = "THIRD-PARTY-NOTICES" },
    { file = "CMakeLists.txt" },
    { file = "native/*/CMakeLists.txt" },
    { file = "native/*/*.cpp" },
    { file = "native/*/*.h" },
    { env = "MACOSX_DEPLOYMENT_TARGET" },
]

[tool.ruff]
line-length = 100
# Private oracle fixtures live outside the committed tree (they are gitignored)
# and are not maintained to the project's lint standard; exclude them so an
# explicit `ruff check src tests` does not fail on files that never ship.
extend-exclude = ["src/moespresso/correctness/fixtures/*/private/"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "real_model: opt-in tests that convert/load locally downloaded full model artifacts",
]
# sentencepiece 0.2.1 (transitive via mlx-lm) ships SWIG C types that lack __module__;
# Python 3.13 raises a DeprecationWarning for those (SwigPyObject/SwigPyPacked at type
# creation, swigvarlink at shutdown). Default Python hides DeprecationWarnings, so serve/
# convert/CLI never showed it, only pytest surfaces it (pytest enables deprecations for
# the session). The upstream issue remains unresolved. Filter only that exact message
# so real deprecations still surface; `-X dev` still shows everything.
filterwarnings = [
    "ignore:builtin type .* has no __module__ attribute:DeprecationWarning",
]
